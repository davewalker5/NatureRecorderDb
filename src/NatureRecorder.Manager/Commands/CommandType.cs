namespace NatureRecorder.Manager.Commands
{
    public enum CommandType
    {
        help,
        adduser,
        categories,
        check,
        connection,
        deleteuser,
        exit,
        export,
        import,
        interactive,
        locations,
        report,
        setpassword,
        species,
        summary,
        update
    }
}
